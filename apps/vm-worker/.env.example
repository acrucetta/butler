ORCH_BASE_URL=http://127.0.0.1:8787

# Required. Must match orchestrator and be 16+ chars.
ORCH_WORKER_TOKEN=replace-with-strong-secret
WORKER_ID=vm-worker-1
WORKER_POLL_MS=2000
WORKER_HEARTBEAT_MS=2000

# Use mock for safe local testing. Switch to rpc in VM.
PI_EXEC_MODE=mock
PI_BINARY=pi
PI_PROVIDER=
PI_MODEL=
PI_WORKSPACE=.data/worker/workspace
PI_SESSION_ROOT=.data/worker/sessions
