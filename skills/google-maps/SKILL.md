# Google Maps

## Purpose
Use this skill for routing, place details, and travel-time checks.

## Constraints
- Clarify departure time assumptions for ETA calculations.
- Ask before storing frequent locations as memory.

## Setup
- Provide API keys in env vars listed in `skill.json`.
- Add MCP server and target entries in `skill.json`.
- Run `npm run butler -- skills sync`.
